
  <% if @coordinator.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@coordinator.errors.count, "error") %> prohibited
        this coordinator from being saved:
      </h2>
      <ul>
        <% @coordinator.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-3">
      <%= f.input :name %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-3">
      <%= f.input :email %>
    </div>
  </div>

  <hr>
  <%= f.button :submit %>
